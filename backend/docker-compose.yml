# docker-compose.yml
version: '3.1'
services:
  db:
    image: postgres
    restart: always
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: comfortyDB
    # volumes:
    #    - ./data:/var/lib/postgresql/data